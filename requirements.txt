# DSCNS runtime dependencies (Python 3.8)
# PyTorch is intentionally NOT listed here: the original experiments used
# torch 1.13.1+cu117 (CUDA 11.7) installed from the conda-forge / PyTorch
# channels. Install it first, e.g.:
#   conda install -n rtdetr pytorch==1.13.1 torchvision==0.14.1 torchaudio==0.13.1 pytorch-cuda=11.7 -c pytorch -c nvidia
# (or follow the official PyTorch installation instructions for your setup).

transformers==4.45.2
datasets==2.21.0
peft==0.12.0
accelerate==0.34.2
huggingface_hub==0.25.2
tokenizers==0.20.1
numpy>=1.20,<1.27
scipy>=1.5
scikit-learn>=1.0
matplotlib>=3.5
pandas>=1.5
pyarrow>=15.0.0
requests>=2.32
tqdm>=4.60
pyyaml>=5.3
